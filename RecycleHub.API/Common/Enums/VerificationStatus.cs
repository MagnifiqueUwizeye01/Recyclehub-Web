namespace RecycleHub.API.Common.Enums
{
    /// <summary>SQL CHECK: VerificationStatus IN ('Pending','Verified','Rejected') — used by SellerProfiles</summary>
    public enum VerificationStatus
    {
        Pending,
        Verified,
        Rejected
    }
}
