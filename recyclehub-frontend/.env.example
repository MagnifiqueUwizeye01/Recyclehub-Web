# Local API (copy to .env.development — that file is gitignored)
VITE_API_BASE_URL=http://localhost:5123/api

# Production builds use .env.production (committed) with the deployed API URL.
# After you deploy the frontend, add its origin (e.g. https://your-app.vercel.app)
# to RecycleHub.API appsettings → Cors:AllowedOrigins so the browser can call the API.
